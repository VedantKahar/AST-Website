/* Container for the map */
.map-container {
    width: 100%;                /* Full width of its parent */
    max-width: 100%;            /* Ensures it doesn't exceed the container's width */
    height: 400px;             /* Fixed height for the box */
    border: 2px solid #34495e; /* Optional: adds a border around the box */
    border-radius: 8px;        /* Optional: rounds the corners of the box */
    overflow: hidden;          /* Ensures the map stays inside the box */
    box-sizing: border-box;    /* Ensures padding and borders are included in the size */
}

#map {
    height: 100%;               /* Full height of the container */
    width: 100%;                /* Full width of the container */
}
*/
<!--<section class="section">
    <h2>Where We Have Worked</h2>
    <div class="map-container">
        <div id="map"></div>
    </div>
</section>-->

<script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
<script>
/*// Initialize map
    var map = L.map('map').setView([20, 0], 2); // Center of the world, zoom level 2


    // Add Tile Layer (OpenStreetMap)
    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
        attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
    }).addTo(map);

    // Set the min and max zoom level to ensure it doesn't zoom out too much
    map.setMinZoom(2);
    map.setMaxZoom(4);  // Zoom level 4 gives a nice world map view without much zooming out

    // Locations where the company has worked (Add more locations here)
    var locations = [
        { name: "India", lat: 20.5937, lon: 78.9629 },
        { name: "USA", lat: 37.0902, lon: -95.7129 },
        { name: "Germany", lat: 51.1657, lon: 10.4515 },
        { name: "Brazil", lat: -14.2350, lon: -51.9253 },
        { name: "Australia", lat: -25.2744, lon: 133.7751 }
    ];

    // Use a custom logo for the marker icon
    var companyIcon = L.icon({
        iconUrl: '../icons/AST-Logo.png',  // Replace this with the path to your company logo
        iconSize: [30, 30],  // Adjust the size of the logo
        iconAnchor: [15, 15], // Anchor the icon to be centered
        popupAnchor: [0, -15] // Adjust popup position relative to icon
    });

    // Add markers for each location with custom logo
    locations.forEach(function(location) {
        L.marker([location.lat, location.lon], { icon: companyIcon })
            .addTo(map)
            .bindPopup('<b>' + location.name + '</b>')
            .openPopup();
    });

    // Optionally, add a zoom control in the top right corner
    map.zoomControl.setPosition('topright');

    // Invalidate map size on page load to fix any display issues
    map.invalidateSize();
*/

</script>

</body>
</html>
